---
- name: Check if reproducer is installed
  stat:
    path: "{{ install_path }}"
  register: reproducer_path

- name: Stop up zuul and friends
  become: true
  command: "{{ ansible_user_dir }}/.local/bin/paunch delete tripleo-ci-reproducer"
